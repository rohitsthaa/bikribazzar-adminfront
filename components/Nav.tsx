// SUPERSEDED — safe to delete: git rm components/Nav.tsx
// Unused top nav bar (only Orders/Products/Gallery links, old gray palette).
// The live admin layout uses components/Sidebar.tsx instead, which has the
// full nav list and current branding. No page imports this file.
export {};
