// SUPERSEDED — safe to delete: git rm "app/(admin)/_dashboard.tsx"
// Stale duplicate of app/(admin)/dashboard/page.tsx (the real dashboard route,
// with charts and server-computed analytics). This underscore-prefixed file
// was never a routable page (Next.js App Router ignores `_`-prefixed files/
// folders) — it's dead code left behind, not something users could ever land
// on. Functional content removed below; delete the file entirely when you get
// a chance.
export {};
