// SUPERSEDED — safe to delete: git rm "app/(admin)/page.tsx.bak"
// The `.bak` extension already keeps Next.js from routing this (it only
// matches an exact `page.tsx`), and its role-based redirect logic
// (super -> /platform, everyone else -> /dashboard) already lives in the
// real app/page.tsx. This is a stray leftover backup file, not live code.
export {};
